version: '3'

services:


  app:
    build:
      context: .
      dockerfile: Dockerfile-app
    networks:
      - app-network
    environment:
      - DB_HOST=${DB_HOST}



  nginx:
    image: nginx:latest
    build:
      context: .
      dockerfile: Dockerfile-serve
    ports:
      - "80:80"
    depends_on:
      - app
    networks:
      - app-network



networks:
  app-network:
    driver: bridge
